# shellcheck shell=bash
# shellcheck disable=SC2034

readonly __BASHKIT_LIB_PODMAN_BUILD_CONSTANTS_SOURCED="true"

readonly ARGFILE_CONF="argfile.conf"

# Reference: podman-build(1)
readonly CONTEXT_CONTAINER_IMAGE="container-image://"

# Reference: podman-build(1)
readonly CONTEXT_CONTAINER_IMAGE_DOCKER_IMAGE="docker-image://"

# Reference: podman-build(1)
readonly CONTEXT_CONTAINER_IMAGE_DOCKER="docker://"

##########################
### Registry constants ###
##########################

readonly LOCAL_REGISTRY="localhost"
readonly REMOTE_REGISTRY="docker.io"

##################
### Image Tags ###
##################

readonly IMAGE_TAG_LATEST="latest"
